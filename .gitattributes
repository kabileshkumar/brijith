# Normalize text files to LF in repo and in working tree
* text=auto eol=lf

# Binary files (never alter line endings)
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.pdf binary
*.zip binary
*.woff binary
*.woff2 binary

# (Optional) Windows-only scripts can keep CRLF
*.bat text eol=crlf
